{
  "name": "grade-tracker",
  "version": "1.0.0",
  "description": "A tracker for students to track their grades.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager . grade-tracker --platform=win32 --arch=x64 --icon=icon.ico --out=../dist/builds --app-version=1.0.0 --overwrite --ignore=node_modules"
  },
  "author": "Xianzhao Duan",
  "license": "MIT",
  "devDependencies": {
    "electron": "^16.0.6",
    "electron-packager": "^15.4.0"
  }
}
